{
  "name": "link-proxy-contracts",
  "scripts": {
    "build": "npm install; forge build; npm run typechain",
    "main": "ts-node ./ts-scripts/main.ts",
    "typechain": "typechain --target ethers-v5 --out-dir ts-scripts/ethers-contracts './out/{Controller,ControllerVault,CustomRouter}.sol/*.json'"
  },
  "dependencies": {
    "@typechain/ethers-v5": "^11.1.2",
    "dotenv": "^16.4.5",
    "ethers": "^*"
  },
  "devDependencies": {
    "-": "^0.0.1",
    "@chainlink/contracts": "^0.6.1",
    "@chainlink/contracts-ccip": "^1.4.0",
    "@chainlink/local": "github:smartcontractkit/chainlink-local",
    "@types/node": "^20.2.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.1.3"
  }
}
